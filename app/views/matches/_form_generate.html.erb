  <%= form_tag({controller: "matches", action: "generate"}, method: "post" ) do |f| %>
  <div class="form-group">
    <%= label_tag(:championship_id, "championship:") %><br>
    <% cities_array = @championships.map { |city| [city.name, city.id] } %>
    <% options= [['Sena 2016', 1], ['Sena 2017', 2]] %>
    <%= select_tag(:championship_id,options_for_select(options),:class=>"form-control") %>
  </div>
  <div class="form-group">
    <%= label_tag(:date_start, "Date start:") %>
    <%= text_field_tag(:date_start,nil,:class => "form-control") %>
  </div>
  <div class="form-group">
     <%= submit_tag("Generar programación",:class=>"btn btn-success") %>
  </div>
<% end %>
